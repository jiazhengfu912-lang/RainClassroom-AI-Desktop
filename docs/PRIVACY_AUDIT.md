# 公开内容隐私审查

审查日期：2026-09-16。对象：本仓库的公开源码、提交历史与 GitHub 内容，以及待发布的 0.1.3 修复。

## 结论

本次检查**未发现真实 API Key、平台 Cookie、课堂令牌、私钥或真实签名题图链接**。这是一份限定范围的审查记录，不是不存在任何泄露的保证。

发现并处理了一项个人信息公开：此前 3 次提交的作者和提交者元数据使用了个人 Gmail 邮箱。本文不重复邮箱地址。经维护者确认，已将主分支历史中的该邮箱替换为 GitHub 提供的 `noreply` 隐私邮箱，并核对重写前后每次提交的文件树、消息和时间均保持不变。当前仓库的后续提交也使用隐私邮箱。

清理范围是当前主分支历史。旧克隆、GitHub 缓存及旧提交链接仍可能保留原邮箱，本次不声称从互联网彻底删除。历史重写会改变提交编号，已有克隆需要留意同步；旧邮箱不属于 API 凭据。

## 已检查的范围

- 远端公开的 `main` 分支及全部 3 次既有提交：提取并扫描 60 个不同的历史文件对象，同时检查提交消息和作者元数据。
- 本次待发布的源码、说明与测试文件；测试中的 `fixture`、`old`、`new` 等是假数据，未使用真实凭据。
- 7 个 Issues、4 条既有 Issue 评论、仓库简介；未发现 PR 审查评论或提交评论。
- 远端未发布标签、Release、Release 附件、Actions 运行或构建附件；未发现可读取的独立 Wiki 仓库。
- 用本地已有的 5 个账号/课堂/题目标识进行精确匹配，未在上述公开内容中发现命中；检查过程不输出这些标识。
- 历史和当前受跟踪文件只包含应用图标，无真实题图、桌面截图、模型设置、平台会话、答题记录或安装包。

使用 Gitleaks 8.30.1 默认规则、100% 脱敏输出扫描源码、完整历史文件内容及公开页面数据，三组均未报密钥命中。另检查邮箱、手机号、Windows 用户目录、JWT、私钥头与签名图片链接。依赖锁文件中命中的第三方维护者公开联系邮箱，以及 GitHub 的 SSH 地址，不属于使用者凭据。

扫描器来自官方发布，下载校验和已核对。原始扫描数据、GitHub 响应和本地报告仅保留在 Git 忽略目录中；本文只发布审查范围与结论。

## 后续保护

`.gitignore` 排除运行数据、研究材料、模型设置、会话、提交记录、环境文件、私钥、Cookie/凭据/令牌 JSON、网络抓包、崩溃转储、截图和安装包。打包文件采用 `dist/**/*` 与 `package.json` 白名单，0.1.3 安装包内容检查通过。

API Key 由 Windows 加密能力保存在本机，不在输入框回显。报告问题时请只提供错误提示和版本，不要粘贴 API Key、Cookie 或完整请求头。发布 Issues 和截图前仍需人工检查。

本次未检查外部克隆、缓存、他人转发或已删除但可能仍被访问的 GitHub 对象；也没有读取 GitHub Actions Secrets 的值。若日后确认真实凭据曾泄露，应先撤销或轮换该凭据，删除文件本身不能使凭据失效。
